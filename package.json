{
  "name": "smartparking",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "cd server && npm install && cd ../client && npm install && npm run build && cd ../server && npm run build",
    "build": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
    "prestart": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
    "start": "cd server && NODE_ENV=production node dist/index.js",
    "dev": "cd server && npm run dev",
    "seed": "cd server && npm run seed"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
